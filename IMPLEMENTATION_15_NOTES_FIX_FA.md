# نسخه ۱۵ — اصلاح دفتر یادداشت

## اصلاح انجام‌شده
- کارت هر یادداشت قابل کلیک شد.
- با لمس یادداشت، پنجره «ویرایش یادداشت» باز می‌شود.
- عنوان و متن قبلی به‌صورت خودکار داخل ویرایشگر قرار می‌گیرد.
- ذخیره تغییرات با همان `uid` و `id` انجام می‌شود؛ یادداشت جدید و تکراری ایجاد نمی‌شود.
- حذف یادداشت همچنان از طریق دکمه حذف انجام می‌شود.
- فهرست یادداشت‌ها با کلید پایدار `id` رندر می‌شود.
- برای متن طولانی، پیش‌نمایش هر یادداشت حداکثر ۳ خط نمایش داده می‌شود و با لمس کارت متن کامل در ویرایشگر باز می‌شود.

## مهاجرت دیتابیس
برای این اصلاح هیچ تغییر در Schema دیتابیس لازم نیست؛ از فیلدها و DAO موجود استفاده شده است.

## جستجوی پیشرفته
قابلیت‌های نسخه ۱۴ حفظ شده‌اند و این نسخه بر همان پایه ساخته شده است.
