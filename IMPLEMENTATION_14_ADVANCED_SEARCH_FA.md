# نسخه ۱۴ — جستجوی پیشرفته

این نسخه بر پایه نسخه نصب‌شده و موفق قبلی توسعه داده شده است و هیچ migration جدیدی برای Room نیاز ندارد.

## امکانات
- جستجوی یکدست‌شده برای متن فارسی: ی/ي، ک/ك، اعراب و نیم‌فاصله.
- سه حالت تطبیق: همه کلمات، عبارت دقیق، هر کلمه.
- فیلتر زنجیره‌ای زمینه ← تعزیه ← نقش.
- جستجو در عنوان بخش، متن، نام نقش، نام تعزیه، زمینه و پانویس.
- مرتب‌سازی بر اساس ارتباط، ساختار یا عنوان.
- امتیازدهی نتایج برای نمایش مرتبط‌ترین موارد در ابتدای فهرست.
- نمایش قطعه‌ای از متن اطراف محل تطبیق و مشخص کردن محل تطبیق.
- جستجوی خودکار با تأخیر کوتاه (debounce) برای جلوگیری از اجرای جستجو در هر ضربه کلید.
- استفاده از یک corpus خوانده‌شده از Room در شروع صفحه برای جلوگیری از Query تکراری در هر حرف.
- حفظ علاقه‌مندی‌ها و مسیر باز شدن مستقیم بخش انتخاب‌شده.

## سازگاری
این قابلیت فقط لایه جستجو و UI را تغییر می‌دهد و ساختار دیتابیس Room را تغییر نمی‌دهد؛ بنابراین برای نصب موجود migration جدید لازم نیست.

## محدودیت فعلی
نتایج گفتگوها همچنان از DAO فعلی استفاده می‌کنند و جداگانه نمایش داده می‌شوند؛ فیلتر زمینه/تعزیه/نقش روی گفتگوها اعمال نمی‌شود.
