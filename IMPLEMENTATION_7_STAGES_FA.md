# گزارش اجرای ۷ مرحله اصلاحات پروژه تعزیه

## 1) Stable UID
- به تمام Entityهای محتوایی و داده‌های کاربر `uid` پایدار اضافه شد.
- `id` عددی Room همچنان برای روابط داخلی باقی مانده است.
- فایل‌های JSON اکنون UID صریح تولید می‌کنند.

## 2) Backup/Restore نسخه 2
- Backup از ID عددی برای روابط استفاده نمی‌کند و از UID استفاده می‌کند.
- Notes, Bookmarks, Recent, Read Sections, Tags, Active Days, My Role, Footnotes و Dialogues پشتیبانی می‌شوند.
- Restore داخل Transaction اجرا می‌شود.
- Restore تکراری برای Note و Dialogue داده تکراری ایجاد نمی‌کند.
- Backup نسخه 1 برای سازگاری قبلی همچنان قابل خواندن است؛ روابط نسخه 1 ذاتاً فقط تا حد ID قدیمی قابل بازیابی‌اند.

## 3) امنیت رمز عبور
- رمز جدید دیگر به صورت Plain Text ذخیره نمی‌شود.
- فرمت جدید `v2$iterations$salt$hash` با PBKDF2-HMAC-SHA256 و salt تصادفی است.
- رمزهای قدیمی در اولین ورود موفق به‌صورت خودکار به فرمت جدید تبدیل می‌شوند.

## 4) Migration امن دیتابیس
- نسخه دیتابیس از 8 به 9 رفت.
- Migration `8 -> 9` بدون حذف داده UIDها را به رکوردهای موجود اضافه می‌کند.
- `fallbackToDestructiveMigration()` حذف شد.
- فقط نسخه‌های قدیمی 1 تا 4 که مسیر Migration آن‌ها در پروژه موجود نیستند، با `fallbackToDestructiveMigrationFrom(1,2,3,4)` بازسازی می‌شوند.
- Schema خروجی Room فعال شد.

## 5) Sync محتوا
- Hash فایل از `hashCode()` به SHA-256 تغییر کرد.
- JSON دارای UID باعث Update واقعی رکورد به‌جای تشخیص بر اساس عنوان می‌شود.
- تغییر عنوان و ترتیب Sectionها پشتیبانی می‌شود.
- `sourceUid` برای Section اضافه شد تا حذف Section از یک فایل، Sectionهای فایل‌های دیگر را حذف نکند.
- ادغام یک منبع چندبار idempotent است.

## 6) اصلاح PDF و Audio و آمار
- متن‌های طولانی PDF بین چند صفحه تقسیم می‌شوند و دیگر نباید از صفحه بیرون بزنند.
- Audio شبکه‌ای فقط HTTPS را می‌پذیرد.
- محاسبه Streak با تاریخ محلی و Calendar انجام می‌شود و به تقسیم ساده میلی‌ثانیه بر 86400000 وابسته نیست.

## 7) تست و پاک‌سازی
- تست Backup برای اختلاف ID مبدأ و مقصد اضافه شد.
- تست Restore تکراری اضافه شد.
- تست Idempotent بودن Sync، تغییر متن/ترتیب و حذف Source-specific اضافه شد.
- تست امنیت Password اضافه شد.
- Bug موجود در `docx_to_json.py` برای فایل‌هایی که با Heading 3/4 شروع می‌شدند اصلاح شد.
- تست‌های Python تبدیل Word به JSON: 4/4 موفق.

## محدودیت محیط بررسی
در این محیط Gradle/Android SDK پروژه در دسترس نبود، بنابراین اجرای کامل `assembleDebug` و تست‌های Android/JVM وابسته به Room از داخل همین محیط ممکن نشد. کد، JSONها و تست Python بررسی شدند و تغییرات برای build استاندارد Gradle پروژه آماده شده‌اند.
