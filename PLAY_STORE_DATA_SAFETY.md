# راهنمای پرکردن فرم Data Safety در Google Play Console

وقتی اپ را برای اولین بار در Play Console منتشر می‌کنید، بخشی به اسم
**App content → Data safety** باید پر شود. این فایل جواب‌های آماده برای
هر سؤال آن فرم است، بر اساس کاری که کد فعلی واقعاً انجام می‌دهد.
(اگر بعداً قابلیتی اضافه/حذف کردید که داده‌ی جدیدی جمع می‌کند، این فایل را
هم به‌روز کنید.)

## آیا اپ داده جمع‌آوری یا اشتراک‌گذاری می‌کند؟
**نه با هیچ سرور یا شرکت ثالثی.** تمام داده‌های زیر فقط **روی گوشی خود
کاربر** ذخیره می‌شوند (Room database + SharedPreferences) و هرگز به
سروری ارسال نمی‌شوند:
- یادداشت‌ها، بوکمارک‌ها، پاورقی‌ها، نقش‌های «من»، گفتگوهای ساخته‌شده
- تنظیمات (تم، فونت، رمز عبور محلی، فاصله خطوط)
- عکس‌هایی که کاربر به گالری هر تعزیه اضافه می‌کند
- فایل صوتی‌ای که کاربر برای یک بخش انتخاب می‌کند

→ در فرم Data Safety، به سؤال «Does your app collect or share any of
the required user data types؟» پاسخ **No** بدهید.

## استثناها (چیزهایی که اپ به بیرون وصل می‌شود)

### ۱. بروزرسانی محتوا از اینترنت
وقتی کاربر از تنظیمات دکمه‌ی «بروزرسانی محتوا از اینترنت» را می‌زند، اپ
یک فایل JSON عمومی (بدون هیچ داده‌ی شخصی کاربر) از یک آدرس مشخص‌شده در
کد (پیش‌فرض: مخزن گیت‌هاب پروژه) **دریافت** می‌کند. هیچ داده‌ای از کاربر
همراه این درخواست ارسال نمی‌شود (فقط یک GET ساده).

### ۲. پشتیبان‌گیری/بازیابی
فایل پشتیبان (JSON شامل یادداشت/بوکمارک/پاورقی/نقش‌من/گفتگو) فقط وقتی
کاربر خودش دکمه‌ی «گرفتن پشتیبان» را بزند، از طریق پنجره‌ی سیستم
اندروید (Storage Access Framework) در مسیری که **خودِ کاربر انتخاب
می‌کند** (حافظه گوشی یا یک سرویس ابری که رویش نصب دارد) ذخیره می‌شود.
اپ مستقیماً به هیچ سرویس ابری متصل نمی‌شود؛ فقط از پنجره‌ی انتخاب فایل
استاندارد اندروید استفاده می‌کند.

### ۳. اعلان‌ها (Notifications)
اعلان «محتوای جدید اضافه شد» کاملاً محلی است (نه از یک سرویس push مثل
Firebase)؛ فقط وقتی محتوای تازه‌ای داخل خود گوشی merge می‌شود نمایش داده
می‌شود.

## اجازه‌های (Permissions) درخواستی و دلیلشان

| اجازه | چرا لازم است |
|---|---|
| `INTERNET` | فقط برای دریافت (نه ارسال) فایل بروزرسانی محتوا |
| `POST_NOTIFICATIONS` (اندروید ۱۳+) | نمایش اعلان محلی «محتوای جدید» |

هیچ اجازه‌ی دیگری (موقعیت مکانی، مخاطبین، دوربین و ...) درخواست نمی‌شود.
دسترسی به گالری/فایل صوتی گوشی از طریق **Storage Access Framework**
(پنجره‌ی انتخاب فایل سیستم) انجام می‌شود که نیاز به اجازه‌ی جداگانه در
Manifest ندارد.

## چک‌لیست سریع قبل از انتشار
- [ ] `PRIVACY_POLICY.md` را با اطلاعات واقعی خودتان پر کرده و یک لینک عمومی
      (مثلاً صفحه‌ای در همین مخزن گیت‌هاب یا یک صفحه‌ی وب) برایش بسازید.
- [ ] همان لینک را در فرم Data Safety و در تنظیمات اپلیکیشن Play Console
      وارد کنید.
- [ ] `local.properties` را طبق `local.properties.example` با کلید امضای
      واقعی خودتان پر کنید (این فایل هرگز نباید commit شود).
- [ ] در «About this app» بخش Play Console، توضیح کوتاه و اسکرین‌شات‌های
      واقعی از اپ (نه از این پروژه) اضافه کنید.
