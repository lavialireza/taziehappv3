# حالت فقط مشاهده

این نسخه برای انتشار عمومی طراحی شده است.

ساخت APK عمومی:
`./gradlew assembleRelease`

ساخت APK فقط مشاهده:
`./gradlew -PviewerApp=true assembleRelease`

در Viewer:
- مدیریت/ویرایش/ورود JSON/Word/خروجی JSON و همگام‌سازی محتوا غیرفعال است.
- مسیرهای مدیریتی مستقیماً در Navigation ساخته نمی‌شوند.
- Backup/Restore سیستم‌عامل برای داده‌های برنامه غیرفعال است.
- Screenshot/Screen Recording با FLAG_SECURE مسدود می‌شود.
- شناسه بسته جداست: `com.example.bookapp.viewer`

نکته امنیتی: این اقدامات دسترسی عادی کاربر را محدود می‌کنند، اما هیچ APK اندرویدی را در برابر دستگاه Root شده، مهندسی معکوس یا استخراج داده از فایل APK/دیتابیس به‌طور مطلق غیرقابل‌دسترسی نمی‌کنند. برای حفاظت جدی از محتوای محرمانه باید رمزنگاری/مدیریت کلید و معماری سروری نیز بررسی شود.
