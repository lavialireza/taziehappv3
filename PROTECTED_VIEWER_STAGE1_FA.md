# مرحله حفاظت واقعی محتوای نسخه عمومی — Stage 1

در این مرحله فقط مسیر محتوای نسخه عمومی تغییر کرده است و سایر امکانات تأییدشده دست‌کاری نشده‌اند.

## تغییرات
- نسخه Admin همچنان فایل JSON عادی خود را دارد.
- نسخه Viewer دیگر `001_sample.json` را به‌صورت متن خام داخل APK ندارد.
- محتوای Viewer به فایل AES-GCM رمزگذاری‌شده با پسوند `.taz` تبدیل شده است.
- Viewer فقط هنگام اجرای برنامه محتوا را در حافظه رمزگشایی می‌کند و سپس همان مسیر قبلی ادغام محتوا را اجرا می‌کند.
- احراز اصالت AES-GCM باعث می‌شود تغییر/دستکاری فایل محافظت‌شده باعث خطای رمزگشایی شود.
- `allowBackup=false` و `FLAG_SECURE` و محدودیت‌های Viewer قبلی حفظ شده‌اند.
- Backup/Restore هیچ تغییری نکرده است.

## نکته امنیتی مهم
این مرحله برای جلوگیری از استخراج ساده فایل JSON از APK و جلوگیری از دستکاری فایل محتوا طراحی شده است. هیچ برنامه‌ای که باید متن را روی دستگاه نمایش دهد نمی‌تواند در برابر دستگاه کاملاً Root شده، دیباگر، Hooking یا Reverse Engineering تضمین «مطلق» بدهد؛ زیرا کلید و متن باید در زمان اجرا در دسترس برنامه قرار بگیرند.

برای سطح امنیت بالاتر از این، مرحله بعدی باید **Provisioning سروری کلید/محتوا** باشد؛ یعنی کلید محتوای Viewer داخل APK قرار نگیرد و پس از احراز دستگاه از سرور دریافت شود.
