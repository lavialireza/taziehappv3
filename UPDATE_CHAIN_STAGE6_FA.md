# مرحله ۶ — مقاوم‌سازی زنجیره بروزرسانی برنامه

- UpdateHelper اکنون تا ۲۰ Release اخیر را بررسی می‌کند.
- Admin فقط `app-admin-release.apk` را ترجیح می‌دهد و در صورت نبود آن برای buildهای آزمایشی به `app-admin-debug.apk` برمی‌گردد.
- Viewer فقط `app-viewer-release.apk` را ترجیح می‌دهد و در صورت نبود آن به `app-viewer-debug.apk` برمی‌گردد.
- Release APK در یک شماره Build یکسان نسبت به Debug اولویت دارد.
- Releaseهای Draft نادیده گرفته می‌شوند.
- استفاده عادی Viewer همچنان آفلاین است و اینترنت فقط هنگام بررسی دستی بروزرسانی مصرف می‌شود.
- Backup/Restore، مدیریت محتوا، Reader، JSON و Word در این مرحله تغییر نکرده‌اند.

## نکته انتشار رسمی
برای بروزرسانی واقعی APK نصب‌شده، Release APK باید با همان کلید امضای نسخه نصب‌شده امضا شود. کلید تست CI فقط برای آزمایش داخلی است.
